# Kairos Agent - Local Agent Dependencies
# Minimal dependencies for Windows activity tracking

# Windows activity tracking (Windows-only)
pywin32>=305; sys_platform == 'win32'
psutil>=5.9.0

# HTTP client for cloud communication
httpx>=0.25.0

# Local API server for UI
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Optional: Windows toast notifications
win10toast>=0.9; sys_platform == 'win32'

# Development/testing
pytest>=7.0.0
